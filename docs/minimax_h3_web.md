# MiniMax H3 Web 使用说明

手机 / 电脑浏览器可用的 **MiniMax H3 生成控制台**（`scripts/minimax_h3_web.py`，端口 `:8191`）。
两级结构「项目 → 任务」，两种生成模式（文生视频 / 参考生视频），串行队列，产物管理，
以及把已生成片段拼接成片的**剪辑时间线**。

- 界面语言：中文（mobile-first，窄屏自动单列）
- 无第三方依赖：Python 标准库 + 原生 HTML/JS（生成/渲染在子进程里跑 `comfyenv`）
- 单页应用，路由为 URL hash：`#/`（项目列表）、`#/p/<项目id>`（项目）、`#/p/<项目id>/edit`（剪辑）

---

## 0. 操作视频

两段录屏演示（真实网页操作，含真实生成 / 真实渲染；无旁白，底部有分步字幕）：

| 视频 | 时长 | 内容 |
|---|---|---|
| `~/Downloads/minimax-h3-demo/videoA-t2v.mp4` | ~1 分钟 | 文生视频全流程：建项目 → 写提示词 → **提示词优化** → 提交 → 队列进度（加速）→ 播放 → 详情 / 复用 |
| `~/Downloads/minimax-h3-demo/videoB-edit.mp4` | ~36 秒 | 剪辑：加片段 → 拖拽排序 → 交叉溶解 / 推进转场 → 2.39:1 画幅 + 淡入淡出 → 导出 → 播放成片 |

> 也可在 **Web 首页的「使用帮助」卡片**直接点开：`用法视频`（内嵌播放，两段可切换）、
> `用法文档`（本页弹窗阅读）。视频随文档放在 `docs/media/`，服务端在 `docs/media/`
> 找不到时会回退到 `~/Downloads/minimax-h3-demo/`。
>
> 用 `mpv` / 浏览器直接打开也可。视频为静音（仅演示界面与流程）。

---

## 1. 打开、启动与状态

### 1.1 访问地址与登录

- 本机：<http://127.0.0.1:8191/>
- 局域网：`http://<本机IP>:8191/`
- 外网（frp 映射）：`http://47.100.49.187:8110/`（`home_minimax_h3` 代理）

使用 **HTTP Basic 认证**：用户名任意，密码为 `scripts/minimax_h3_web.py` 里的
`DEFAULT_PASSWORD`（当前 `aiakos-ref2v`，启动时可用 `--password <新密码>` 修改）。
浏览器会弹一次登录框，之后本会话免输。

### 1.2 启动 / 停止

```bash
# 启动（后台，默认 0.0.0.0:8191）
python3 ~/MiniMax-H3-Deploy/scripts/minimax_h3_web.py --start
python3 ~/MiniMax-H3-Deploy/scripts/minimax_h3_web.py --status
python3 ~/MiniMax-H3-Deploy/scripts/minimax_h3_web.py --stop
```

- 日志：`~/MiniMax-H3-Deploy/minimax_h3_web.log`
- 数据目录：`~/MiniMax-H3-Deploy/.h3ref2v/`（任务、项目、时间线序列）

### 1.3 依赖：ComfyUI 引擎

生成需要 **ComfyUI 引擎在 `:8188` 运行**（`scripts/start-comfyui-for-minimax-h3.sh`）。
顶部状态条会显示 `ComfyUI 在线 / 离线`：

- 生成结束后模型**常驻显存**，以便下次快速生成；
- 需要腾显存时点右上角 **「释放显存」**（会停止 ComfyUI，下次生成需冷启动，约多 1~2 分钟）。

也可以用系统面板（`http://<本机IP>:8200/`）的 **「MiniMax H3」卡片**启停本控制台，卡片的
「面板 :8191」链接直达本页。

---

## 2. 项目

打开首页即项目列表（卡片显示项目名、任务数、封面缩略图）。

- **新建项目**：顶部「新建项目」输入名称 → 「创建」。
- **进入项目**：点项目卡片。
- **进入项目后**右上角有：`剪辑`（进入时间线）、`改名`、`删除`、`全部项目`。
- **删除项目**弹窗可选：
  - 勾选「同时删除产物」→ **purge**：删除项目、任务记录与该项目的视频文件（不可恢复）；
  - 取消勾选 → **detach**：项目内任务转回「默认项目」，视频文件保留。
- `默认项目`（id `default`）是内置项目，不能改名 / 删除。

> 建议按作品/题材建项目，产物按 `output/<项目id>/<模式>/<任务id>.mp4` 归档，互不干扰。

---

## 3. 新建任务

进入项目后，左侧「新建任务」卡片：

### 3.1 模式：文生视频（t2v）

- 顶部下拉选 **「文生视频」**。
- 只填提示词即可（纯文本生视频）；
- 可选上传 **首帧** / **尾帧**（各一张图片）做画面锚定。

### 3.2 模式：参考生视频（ref2v）

- 模式选 **「参考生视频」**，然后上传参考素材：
  - **参考图**（≤9 张）、**参考视频**（≤3 段）、**参考音频**（≤3 段）；
  - 至少上传 1 个参考素材。
- 上传后在素材右侧标签按钮点一下，会把引用标记（如 `<Picture 1>` / `<Video 1>` / `<Audio 1>`）
  插入提示词光标处，用于指明「哪张图 / 哪段视频」参与；不插入则默认整体参考。

### 3.3 提示词与「提示词优化」

- 在提示词框写画面描述（中文/英文均可）。
- 点 **「提示词优化」**：调用云端大模型，把你的描述扩写为**电影级分镜提示词**
  （镜头运动、景别、光线、氛围、画质关键词，并结合已上传的参考素材种类）。
- 弹窗中可 **「复制」** 或 **「替换输入框」**（用优化结果覆盖原提示词）。
- 未配置 API key 会提示去 `~/.config/h3ref2v/llm.conf` 配置（见 §7）。

### 3.4 参数

| 参数 | 说明 |
|---|---|
| 时长(秒) | 1–15，生成视频的目标秒数（实际帧数按模型 24fps 规则就近对齐） |
| 步数 | 1–50，采样步数；越高质量越好、越慢（默认 8） |
| seed | 留空 = **随机**；填数字可复现 |
| 画幅 | 16:9 / 9:16 / 1:1 等 |
| 分辨率(MP) | 0.2–0.8 百万像素（默认 0.4） |
| 参考图缩放 | 仅 ref2v：`match`（快）/ `max`（保真） |

### 3.5 提交 / 重置

- **提交**：先弹确认框（项目、类型、时长、步数、素材、提示词摘要），确认后上传并排队。
- **重置**：弹确认框后清空表单并恢复默认（t2v / 5s / 8 步 / 随机 seed / 16:9 / 0.4MP）。
- 队列**全局串行**（一次只跑一个生成任务，保证显存安全）。

---

## 4. 任务与产物（右侧栏）

- **当前任务**：显示正在跑/排队任务的状态、阶段（如「加载/解码 VAE」「采样」）、进度条、已用时、seed。
- **诊断日志**（折叠）：实时生成日志，排障用。
- **任务记录**：该项目的历史任务，包含任务 id、类型、素材、用时与状态。每行按钮：
  - **详情**：查看完整参数（时长/步数/seed/画幅/分辨率）与**素材预览**（图/视频/音频，可播放）。
  - **复用**：把该任务的参数与素材**一键回填**到「新建任务」表单（**seed 留空 = 随机**，
    避免复现同一段，需手动改参数后自行提交）。
  - **取消 / 删除**；已完成的任务有 **查看**（播放产物）。
- **产物**：按项目列出的成片缩略图。
  - 点缩略图直接**播放**；
  - 点标题栏 **「编辑」** → 进入多选模式：勾选后「删除」可批量删除（有确认弹窗）。
- 状态含义：排队中 / 进行中 / 已完成 / 失败 / 已取消 / 已中断（web 重启导致，重新提交即可）。

---

## 5. 剪辑成片（时间线）

项目页右上角 **「剪辑」** 进入 `#/p/<项目id>/edit`。把该项目里已生成的片段拼接成一个成片。

1. **添加片段**：下方「添加片段」点缩略图，依次加入时间线（可重复加同一段）。
2. **排序**：拖拽每段左侧的 **`≡`** 手柄；或用 **▲ / ▼** 微调；**✕** 移除。
3. **转场**：相邻片段之间的「转场」下拉，四种：
   - `硬切`（无重叠）、`黑场渐隐`、`交叉溶解`、`推进/滑动`；
   - 非硬切可选转场时长 `0.3 / 0.5 / 0.8 / 1.0 / 1.5s`。
4. **成片设置**：`成片画幅`（原始 / 2.39:1 / 16:9 / 9:16 / 1:1 / 4:3 / 3:4，按首段中心裁切统一）、
   `片头淡入` / `片尾淡出`（0–3s）。
5. **保存**：把时间线写入 `output/<项目id>/edit/` 同级的序列文件（项目内 `edit.json`）。
6. **预览/导出** → 确认后提交渲染（排队执行）。渲染进程实时显示进度与日志。
7. **成片**列表出现结果后可播放；左下角红色按钮删除成片（连同文件）。

- 输出：`~/MiniMax-H3-Deploy/output/<项目id>/edit/<任务id>.mp4`
- 成片**不计入**「产物」和项目任务数，也不会出现在「任务记录」里（按 `mode=edit` 过滤）。
- 渲染为纯 CPU（PyAV/libx264），逐帧拼接、按首段尺寸裁幅、逐段音频对齐 + 硬切处防爆音淡化。

---

## 6. 素材规格与限制

| 项目 | 规格 |
|---|---|
| 参考图 | ≤ 9 张 |
| 参考视频 | ≤ 3 段；**建议每段 2–15s、合计 ≤ 15s** |
| 参考音频 | ≤ 3 段；**建议合计 ≤ 15s** |
| 首帧 / 尾帧 | 各 1 张图片（t2v 可选） |
| 时长 / 步数 | 1–15s / 1–50 |
| 输出帧率 | 24 fps（时长会就近对齐到模型支持的帧数） |

---

## 7. 提示词优化配置

编辑 `~/.config/h3ref2v/llm.conf`（权限 600，OpenAI 兼容接口）：

```ini
base=https://api.deepseek.com/v1
model=deepseek-chat
key=sk-...
```

也可用环境变量覆盖：`REF2V_LLM_BASE` / `REF2V_LLM_MODEL` / `REF2V_LLM_KEY`（文件优先）。

---

## 8. 数据与目录

```
~/MiniMax-H3-Deploy/
├── minimax_h3_web.log              # 控制台日志
├── .h3ref2v/
│   ├── minimax_h3_web.pid          # pidfile
│   ├── projects/<pid>/project.json # 项目元数据
│   ├── projects/<pid>/edit.json    # 该项目的剪辑时间线
│   └── jobs/<jid>/{config.json,status.json,log.txt,uploads/}
└── output/<pid>/<t2v|ref2v|edit>/<jid>.mp4   # 生成产物 / 成片
```

（旧版遗留路径 `output/<mode>/<jid>.mp4` 仍可被读取显示。）

---

## 9. 常见问题

- **顶部显示「ComfyUI 离线」**：生成前先启动引擎
  （`bash ~/MiniMax-H3-Deploy/scripts/start-comfyui-for-minimax-h3.sh` 或系统面板 Qwen/ComfyUI 卡片）。
- **任务失败 / 显存不足**：点「释放显存」停止 ComfyUI 后重试；避免与其他大模型服务同时占用显存。
- **任务显示「已中断」**：web 服务重启会把运行中的任务标记为中断，重新提交即可。
- **提示词优化失败**：检查 `llm.conf` 的 key/网络；未配置时手动写提示词亦可。
- **上传被拒**：参考图/视频/音频数量超限（§6），或 ref2v 未上传任何素材、t2v 上传了参考素材。
- **手机访问**：直接用局域网 `http://<IP>:8191/`（或 frp `8110`），页面为响应式布局。

---

## 10. 进阶：HTTP API 速查

同 Basic 认证；`<user>` 任意。

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/state` | ComfyUI 状态、显存、当前/排队任务 |
| GET | `/api/projects` | 项目列表 |
| POST | `/api/projects` | 新建项目 `{name}` |
| POST | `/api/projects/<id>/rename` / `delete` | 改名 / 删除 `{name}` 或 `{mode:purge|detach}` |
| GET | `/api/jobs?project=<pid>` | 任务列表（含参数/素材） |
| GET | `/api/jobs/<jid>` / `/log?offset=` | 单任务详情 / 增量日志 |
| POST | `/api/jobs/<jid>/cancel` / `delete` | 取消 / 删除记录 |
| GET | `/api/outputs?project=<pid>` | 产物列表 |
| POST | `/api/output/delete` | 删除产物 `{rels:[...]}` |
| POST | `/api/optimize` | 提示词优化 `{prompt, counts}` |
| POST | `/api/run` | 提交生成（multipart：project/mode/prompt/dur/steps/aspect/megapixels + 素材） |
| GET/POST | `/api/sequence?project=<pid>` | 读取 / 保存剪辑序列 |
| POST | `/api/edit/render` | 提交成片渲染 `{project}` |
| POST | `/api/edit/delete` | 删除成片 `{id}` |
| GET | `/media/<jid>/<file>`、`/files/<rel>` | 素材回放 / 产物回放 |
